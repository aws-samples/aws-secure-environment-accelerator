export function createConfigRecorderName(acceleratorPrefix: string): string {
  return `${acceleratorPrefix}Config-Recorder`;
}

export function createAggregatorName(acceleratorPrefix: string): string {
  return `${acceleratorPrefix}Org-Aggregator`;
}
