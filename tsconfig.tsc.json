{
  "extends": "./tsconfig.base.json",
  "include": [
    "src/**/*",
  ],
  "exclude": [
    // We ignore files at the root of the cdk folder and not subfolders here
    "src/deployments/cdk/*",
    // TODO Add back tsc for the UI project at one point
    "src/ui/**/*",
    "src/core/runtime/test/*"
  ]
}